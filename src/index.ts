import filter from './filter';
import get from './get';
import reduce from './reduce';
import map from './map';
import each from './each';
import every from './every';
import concat from './concat';
import first from './first';

export { filter, get, reduce, map, each, every, concat, first };

export default { filter, get, reduce, map, each, every, concat, first };

