import filter from './filter'
import get from './get'
import reduce from './reduce'
import map from './map'
import each from './each'
import every from './every'

export { filter, get, reduce, map, each, every }

export default { filter, get, reduce, map, each, every }

