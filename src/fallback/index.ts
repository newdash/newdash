import { fallbackCache } from './cache';
import { fallbackRetry } from './retry';

/**
 * fallback namespace
 * @since 5.15.0
 */
export const fallback = {
  cache: fallbackCache,
  retry: fallbackRetry
};


