import filter from './filter'
import get from './get'
import reduce from './reduce'
import map from './map'
import each from './each'

export { filter, get, reduce, map, each }

export default { filter, get, reduce, map, each }

